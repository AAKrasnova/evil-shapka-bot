CREATE TABLE users (
	id varchar
);

CREATE TABLE knowledge (
	id varchar,
	name varchar,
	adder varchar,
	type varchar,
	subtype varchar,
	theme varchar,
	sphere varchar,
	link text,
	word_count integer,
	duration integer
);
